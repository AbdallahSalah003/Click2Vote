import { useNavigate } from "react-router-dom";

export function WaitingRoomPage() {
    const navigate = useNavigate();


    return (
        <>
            <h2>Welcome, to Wating Room!</h2>
        </>
    );
}